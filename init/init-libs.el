
;; COMMON LISP
(require 'cl)
(use-package noflet)

;; ORG
(require 'org)

;; ASYNC
;; (require 'async)
;; (require 'async-file)

;; OBJECT MANIPULATION
(use-package s)
(use-package dash)
(use-package prf-string
  :load-path "~/.emacs.d/plugins/prf-string")

(provide 'init-libs)
