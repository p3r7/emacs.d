
;; TODO: has to be patched to work w/ azerty keyboard (pretty bad implementation on that point)

;; (autoload 'chord-mode	"tablature-mode"   "Guitar tablature."  t)

(use-package tablature-mode
  :load-path "~/.emacs.d/plugins/Tab-n-Fret"
  ;; :commands (tab-mode chord-mode lead-mode)
  )




(provide 'init-guitar-tab)
